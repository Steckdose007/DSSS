from setuptools import setup

setup(
   name='snowflake1',
   version='0.0',
   description='Creates a window in which randomly colored snowflakes are drawn',
   author='Florian Gritsch',
   author_email='DSSS@FAU.de',
   packages=['snowflake1'],  # would be the same as name
   install_requires= None, #external packages acting as dependencies
)